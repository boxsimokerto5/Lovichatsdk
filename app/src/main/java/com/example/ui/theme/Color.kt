package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Lovy Chat vibrant coral berry and modern violet palette (Cerah, Fresh, Bersih)
val LovyPrimary = Color(0xFFFF3366)        // Vibrant energetic rose / coral pink
val LovyPrimaryVariant = Color(0xFFE0245E)
val LovySecondary = Color(0xFF7C3AED)      // Rich purple accent
val LovySecondaryVariant = Color(0xFF6D28D9)
val LovyTertiary = Color(0xFFFF85A1)       // Soft pastel rose
val LovyAccent = Color(0xFF06B6D4)         // Bright fresh cyan

// Pristine, luminous light background & surfaces
val LovyLightBackground = Color(0xFFF8F9FC)       // Crisp soft white canvas
val LovyLightSurface = Color(0xFFFFFFFF)          // Pure white cards & containers
val LovyLightSurfaceVariant = Color(0xFFEFF2F8)   // Very soft pastel tint for chips & inputs
val LovyLightBorder = Color(0xFFE5E7EB)           // Subtle elegant divider

// Deep tones for high-contrast crisp typography on light canvas
val LovyTextPrimary = Color(0xFF191C24)           // Clear high-contrast dark slate
val LovyTextSecondary = Color(0xFF5A6275)         // Readable medium slate
val LovyTextMuted = Color(0xFF8C95A8)             // Subtle placeholder slate

// Status indicators (vivid & clear)
val OnlineGreen = Color(0xFF10B981)               // Vivid emerald green
val EncryptedGold = Color(0xFFF59E0B)             // Amber gold
val SecurityShieldBlue = Color(0xFF3B82F6)        // Bright blue

